import React, { useRef } from 'react';

const UpButton = () => {
  return (
    <div>
      <button className="fixed p-5 bg-black text-white rounded-full hover:bg-gray-800 transition-all duration-300">up</button>
    </div>
  );
};

export default UpButton;
